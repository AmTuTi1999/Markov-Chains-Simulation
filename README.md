# Markov Chains Simulation
This repository contains a simple implementation of Markov Chains in Python. It allows you to create a Markov Chain model, train it with data, and generate sequences based on the learned probabilities.
## Features
- Create Markov Chain models of arbitrary order.
- Train models using input sequences.
- Generate new sequences based on the trained model.